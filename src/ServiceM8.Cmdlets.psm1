<#
    ServiceM8.Cmdlets
    Typed cmdlets over the ServiceM8 engine.

    AUTO-GENERATED by scripts/Generate-Module.ps1 from the resource registry.
    Do not edit by hand -- update the registry and regenerate.
    Each cmdlet delegates to Invoke-Sm8Resource (the single portable CRUD entry point).
#>

Set-StrictMode -Version Latest


# ---------------------------------------------------------------------------
# AllocationWindow
# ---------------------------------------------------------------------------

function Get-Sm8AllocationWindow {
    <#
    .SYNOPSIS
        List or retrieve AllocationWindow records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8AllocationWindow -All
    .EXAMPLE
        Get-Sm8AllocationWindow -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "AllocationWindow" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8AllocationWindow {
    <#
    .SYNOPSIS
        Create a new AllocationWindow.
    .EXAMPLE
        New-Sm8AllocationWindow -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "AllocationWindow" -Verb Post -Body $Body
}

function Update-Sm8AllocationWindow {
    <#
    .SYNOPSIS
        Update an existing AllocationWindow.
    .EXAMPLE
        Update-Sm8AllocationWindow -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "AllocationWindow" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8AllocationWindow {
    <#
    .SYNOPSIS
        Delete (deactivate) a AllocationWindow record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "AllocationWindow" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_schedule, create: manage_schedule, delete/update: manage_schedule


# ---------------------------------------------------------------------------
# Asset
# ---------------------------------------------------------------------------

function Get-Sm8Asset {
    <#
    .SYNOPSIS
        List or retrieve Asset records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Asset -All
    .EXAMPLE
        Get-Sm8Asset -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Asset" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Asset {
    <#
    .SYNOPSIS
        Create a new Asset.
    .EXAMPLE
        New-Sm8Asset -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Asset" -Verb Post -Body $Body
}

function Update-Sm8Asset {
    <#
    .SYNOPSIS
        Update an existing Asset.
    .EXAMPLE
        Update-Sm8Asset -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Asset" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Asset {
    <#
    .SYNOPSIS
        Delete (deactivate) a Asset record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Asset" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_assets, create: manage_assets, delete/update: manage_assets


# ---------------------------------------------------------------------------
# AssetType
# ---------------------------------------------------------------------------

function Get-Sm8AssetType {
    <#
    .SYNOPSIS
        List or retrieve AssetType records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8AssetType -All
    .EXAMPLE
        Get-Sm8AssetType -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "AssetType" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8AssetType {
    <#
    .SYNOPSIS
        Create a new AssetType.
    .EXAMPLE
        New-Sm8AssetType -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "AssetType" -Verb Post -Body $Body
}

function Update-Sm8AssetType {
    <#
    .SYNOPSIS
        Update an existing AssetType.
    .EXAMPLE
        Update-Sm8AssetType -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "AssetType" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8AssetType {
    <#
    .SYNOPSIS
        Delete (deactivate) a AssetType record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "AssetType" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_assets, create: manage_assets, delete/update: manage_assets


# ---------------------------------------------------------------------------
# AssetTypeField
# ---------------------------------------------------------------------------

function Get-Sm8AssetTypeField {
    <#
    .SYNOPSIS
        List or retrieve AssetTypeField records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8AssetTypeField -All
    .EXAMPLE
        Get-Sm8AssetTypeField -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "AssetTypeField" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8AssetTypeField {
    <#
    .SYNOPSIS
        Create a new AssetTypeField.
    .EXAMPLE
        New-Sm8AssetTypeField -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "AssetTypeField" -Verb Post -Body $Body
}

function Update-Sm8AssetTypeField {
    <#
    .SYNOPSIS
        Update an existing AssetTypeField.
    .EXAMPLE
        Update-Sm8AssetTypeField -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "AssetTypeField" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8AssetTypeField {
    <#
    .SYNOPSIS
        Delete (deactivate) a AssetTypeField record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "AssetTypeField" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_assets, create: manage_assets, delete/update: manage_assets


# ---------------------------------------------------------------------------
# Attachment
# ---------------------------------------------------------------------------

function Get-Sm8Attachment {
    <#
    .SYNOPSIS
        List or retrieve Attachment records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Attachment -All
    .EXAMPLE
        Get-Sm8Attachment -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Attachment" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Attachment {
    <#
    .SYNOPSIS
        Create a new Attachment.
    .EXAMPLE
        New-Sm8Attachment -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Attachment" -Verb Post -Body $Body
}

function Update-Sm8Attachment {
    <#
    .SYNOPSIS
        Update an existing Attachment.
    .EXAMPLE
        Update-Sm8Attachment -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Attachment" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Attachment {
    <#
    .SYNOPSIS
        Delete (deactivate) a Attachment record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Attachment" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_attachments, create: manage_attachments, delete/update: manage_attachments


# ---------------------------------------------------------------------------
# Availability
# ---------------------------------------------------------------------------

function Get-Sm8Availability {
    <#
    .SYNOPSIS
        List or retrieve Availability records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Availability -All
    .EXAMPLE
        Get-Sm8Availability -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Availability" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Availability {
    <#
    .SYNOPSIS
        Create a new Availability.
    .EXAMPLE
        New-Sm8Availability -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Availability" -Verb Post -Body $Body
}

function Update-Sm8Availability {
    <#
    .SYNOPSIS
        Update an existing Availability.
    .EXAMPLE
        Update-Sm8Availability -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Availability" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Availability {
    <#
    .SYNOPSIS
        Delete (deactivate) a Availability record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Availability" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_schedule, create: manage_schedule, delete/update: manage_schedule


# ---------------------------------------------------------------------------
# Badge
# ---------------------------------------------------------------------------

function Get-Sm8Badge {
    <#
    .SYNOPSIS
        List or retrieve Badge records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Badge -All
    .EXAMPLE
        Get-Sm8Badge -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Badge" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

    # scopes -> read: manage_badges, create: manage_badges, delete/update: manage_badges


# ---------------------------------------------------------------------------
# Bundle
# ---------------------------------------------------------------------------

function Get-Sm8Bundle {
    <#
    .SYNOPSIS
        List or retrieve Bundle records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Bundle -All
    .EXAMPLE
        Get-Sm8Bundle -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Bundle" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Bundle {
    <#
    .SYNOPSIS
        Create a new Bundle.
    .EXAMPLE
        New-Sm8Bundle -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Bundle" -Verb Post -Body $Body
}

function Update-Sm8Bundle {
    <#
    .SYNOPSIS
        Update an existing Bundle.
    .EXAMPLE
        Update-Sm8Bundle -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Bundle" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Bundle {
    <#
    .SYNOPSIS
        Delete (deactivate) a Bundle record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Bundle" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_inventory, create: manage_inventory, delete/update: manage_inventory


# ---------------------------------------------------------------------------
# Category
# ---------------------------------------------------------------------------

function Get-Sm8Category {
    <#
    .SYNOPSIS
        List or retrieve Category records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Category -All
    .EXAMPLE
        Get-Sm8Category -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Category" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Category {
    <#
    .SYNOPSIS
        Create a new Category.
    .EXAMPLE
        New-Sm8Category -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Category" -Verb Post -Body $Body
}

function Update-Sm8Category {
    <#
    .SYNOPSIS
        Update an existing Category.
    .EXAMPLE
        Update-Sm8Category -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Category" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Category {
    <#
    .SYNOPSIS
        Delete (deactivate) a Category record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Category" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_job_categories, create: manage_job_categories, delete/update: manage_job_categories


# ---------------------------------------------------------------------------
# Client
# ---------------------------------------------------------------------------

function Get-Sm8Client {
    <#
    .SYNOPSIS
        List or retrieve Client records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Client -All
    .EXAMPLE
        Get-Sm8Client -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Client" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Client {
    <#
    .SYNOPSIS
        Create a new Client.
    .EXAMPLE
        New-Sm8Client -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Client" -Verb Post -Body $Body
}

function Update-Sm8Client {
    <#
    .SYNOPSIS
        Update an existing Client.
    .EXAMPLE
        Update-Sm8Client -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Client" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Client {
    <#
    .SYNOPSIS
        Delete (deactivate) a Client record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Client" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_customers, create: manage_customers, delete/update: manage_customers


# ---------------------------------------------------------------------------
# Company
# ---------------------------------------------------------------------------

function Get-Sm8Company {
    <#
    .SYNOPSIS
        List or retrieve Company records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Company -All
    .EXAMPLE
        Get-Sm8Company -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Company" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Company {
    <#
    .SYNOPSIS
        Create a new Company.
    .EXAMPLE
        New-Sm8Company -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Company" -Verb Post -Body $Body
}

function Update-Sm8Company {
    <#
    .SYNOPSIS
        Update an existing Company.
    .EXAMPLE
        Update-Sm8Company -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Company" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Company {
    <#
    .SYNOPSIS
        Delete (deactivate) a Company record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Company" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_customers, create: manage_customers, delete/update: manage_customers


# ---------------------------------------------------------------------------
# CompanyContact
# ---------------------------------------------------------------------------

function Get-Sm8CompanyContact {
    <#
    .SYNOPSIS
        List or retrieve CompanyContact records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8CompanyContact -All
    .EXAMPLE
        Get-Sm8CompanyContact -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "CompanyContact" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8CompanyContact {
    <#
    .SYNOPSIS
        Create a new CompanyContact.
    .EXAMPLE
        New-Sm8CompanyContact -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "CompanyContact" -Verb Post -Body $Body
}

function Update-Sm8CompanyContact {
    <#
    .SYNOPSIS
        Update an existing CompanyContact.
    .EXAMPLE
        Update-Sm8CompanyContact -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "CompanyContact" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8CompanyContact {
    <#
    .SYNOPSIS
        Delete (deactivate) a CompanyContact record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "CompanyContact" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_customer_contacts, create: manage_customer_contacts, delete/update: manage_customer_contacts


# ---------------------------------------------------------------------------
# DocumentTemplate
# ---------------------------------------------------------------------------

function Get-Sm8DocumentTemplate {
    <#
    .SYNOPSIS
        List or retrieve DocumentTemplate records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8DocumentTemplate -All
    .EXAMPLE
        Get-Sm8DocumentTemplate -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "DocumentTemplate" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

    # scopes -> read: manage_templates, create: manage_templates, delete/update: manage_templates


# ---------------------------------------------------------------------------
# EmailTemplate
# ---------------------------------------------------------------------------

function Get-Sm8EmailTemplate {
    <#
    .SYNOPSIS
        List or retrieve EmailTemplate records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8EmailTemplate -All
    .EXAMPLE
        Get-Sm8EmailTemplate -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "EmailTemplate" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

    # scopes -> read: manage_templates, create: manage_templates, delete/update: manage_templates


# ---------------------------------------------------------------------------
# Feedback
# ---------------------------------------------------------------------------

function Get-Sm8Feedback {
    <#
    .SYNOPSIS
        List or retrieve Feedback records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Feedback -All
    .EXAMPLE
        Get-Sm8Feedback -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Feedback" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Feedback {
    <#
    .SYNOPSIS
        Create a new Feedback.
    .EXAMPLE
        New-Sm8Feedback -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Feedback" -Verb Post -Body $Body
}

function Update-Sm8Feedback {
    <#
    .SYNOPSIS
        Update an existing Feedback.
    .EXAMPLE
        Update-Sm8Feedback -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Feedback" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Feedback {
    <#
    .SYNOPSIS
        Delete (deactivate) a Feedback record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Feedback" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_feedback, create: manage_feedback, delete/update: manage_feedback


# ---------------------------------------------------------------------------
# Form
# ---------------------------------------------------------------------------

function Get-Sm8Form {
    <#
    .SYNOPSIS
        List or retrieve Form records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Form -All
    .EXAMPLE
        Get-Sm8Form -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Form" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Form {
    <#
    .SYNOPSIS
        Create a new Form.
    .EXAMPLE
        New-Sm8Form -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Form" -Verb Post -Body $Body
}

function Update-Sm8Form {
    <#
    .SYNOPSIS
        Update an existing Form.
    .EXAMPLE
        Update-Sm8Form -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Form" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Form {
    <#
    .SYNOPSIS
        Delete (deactivate) a Form record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Form" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_forms, create: manage_forms, delete/update: manage_forms


# ---------------------------------------------------------------------------
# FormField
# ---------------------------------------------------------------------------

function Get-Sm8FormField {
    <#
    .SYNOPSIS
        List or retrieve FormField records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8FormField -All
    .EXAMPLE
        Get-Sm8FormField -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "FormField" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8FormField {
    <#
    .SYNOPSIS
        Create a new FormField.
    .EXAMPLE
        New-Sm8FormField -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "FormField" -Verb Post -Body $Body
}

function Update-Sm8FormField {
    <#
    .SYNOPSIS
        Update an existing FormField.
    .EXAMPLE
        Update-Sm8FormField -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "FormField" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8FormField {
    <#
    .SYNOPSIS
        Delete (deactivate) a FormField record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "FormField" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_forms, create: manage_forms, delete/update: manage_forms


# ---------------------------------------------------------------------------
# FormResponse
# ---------------------------------------------------------------------------

function Get-Sm8FormResponse {
    <#
    .SYNOPSIS
        List or retrieve FormResponse records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8FormResponse -All
    .EXAMPLE
        Get-Sm8FormResponse -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "FormResponse" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8FormResponse {
    <#
    .SYNOPSIS
        Create a new FormResponse.
    .EXAMPLE
        New-Sm8FormResponse -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "FormResponse" -Verb Post -Body $Body
}

function Update-Sm8FormResponse {
    <#
    .SYNOPSIS
        Update an existing FormResponse.
    .EXAMPLE
        Update-Sm8FormResponse -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "FormResponse" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8FormResponse {
    <#
    .SYNOPSIS
        Delete (deactivate) a FormResponse record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "FormResponse" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_forms, create: manage_forms, delete/update: manage_forms


# ---------------------------------------------------------------------------
# Job
# ---------------------------------------------------------------------------

function Get-Sm8Job {
    <#
    .SYNOPSIS
        List or retrieve Job records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Job -All
    .EXAMPLE
        Get-Sm8Job -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Job" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Job {
    <#
    .SYNOPSIS
        Create a new Job.
    .EXAMPLE
        New-Sm8Job -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Job" -Verb Post -Body $Body
}

function Update-Sm8Job {
    <#
    .SYNOPSIS
        Update an existing Job.
    .EXAMPLE
        Update-Sm8Job -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Job" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Job {
    <#
    .SYNOPSIS
        Delete (deactivate) a Job record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Job" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_jobs, create: create_jobs, delete/update: manage_jobs


# ---------------------------------------------------------------------------
# JobActivity
# ---------------------------------------------------------------------------

function Get-Sm8JobActivity {
    <#
    .SYNOPSIS
        List or retrieve JobActivity records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8JobActivity -All
    .EXAMPLE
        Get-Sm8JobActivity -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "JobActivity" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8JobActivity {
    <#
    .SYNOPSIS
        Create a new JobActivity.
    .EXAMPLE
        New-Sm8JobActivity -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobActivity" -Verb Post -Body $Body
}

function Update-Sm8JobActivity {
    <#
    .SYNOPSIS
        Update an existing JobActivity.
    .EXAMPLE
        Update-Sm8JobActivity -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobActivity" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8JobActivity {
    <#
    .SYNOPSIS
        Delete (deactivate) a JobActivity record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "JobActivity" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_schedule, create: manage_schedule, delete/update: manage_schedule


# ---------------------------------------------------------------------------
# JobAllocation
# ---------------------------------------------------------------------------

function Get-Sm8JobAllocation {
    <#
    .SYNOPSIS
        List or retrieve JobAllocation records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8JobAllocation -All
    .EXAMPLE
        Get-Sm8JobAllocation -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "JobAllocation" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8JobAllocation {
    <#
    .SYNOPSIS
        Create a new JobAllocation.
    .EXAMPLE
        New-Sm8JobAllocation -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobAllocation" -Verb Post -Body $Body
}

function Update-Sm8JobAllocation {
    <#
    .SYNOPSIS
        Update an existing JobAllocation.
    .EXAMPLE
        Update-Sm8JobAllocation -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobAllocation" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8JobAllocation {
    <#
    .SYNOPSIS
        Delete (deactivate) a JobAllocation record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "JobAllocation" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_schedule, create: manage_schedule, delete/update: manage_schedule


# ---------------------------------------------------------------------------
# JobChecklist
# ---------------------------------------------------------------------------

function Get-Sm8JobChecklist {
    <#
    .SYNOPSIS
        List or retrieve JobChecklist records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8JobChecklist -All
    .EXAMPLE
        Get-Sm8JobChecklist -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "JobChecklist" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8JobChecklist {
    <#
    .SYNOPSIS
        Create a new JobChecklist.
    .EXAMPLE
        New-Sm8JobChecklist -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobChecklist" -Verb Post -Body $Body
}

function Update-Sm8JobChecklist {
    <#
    .SYNOPSIS
        Update an existing JobChecklist.
    .EXAMPLE
        Update-Sm8JobChecklist -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobChecklist" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8JobChecklist {
    <#
    .SYNOPSIS
        Delete (deactivate) a JobChecklist record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "JobChecklist" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_job_checklists, create: manage_job_checklists, delete/update: manage_job_checklists


# ---------------------------------------------------------------------------
# JobContact
# ---------------------------------------------------------------------------

function Get-Sm8JobContact {
    <#
    .SYNOPSIS
        List or retrieve JobContact records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8JobContact -All
    .EXAMPLE
        Get-Sm8JobContact -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "JobContact" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8JobContact {
    <#
    .SYNOPSIS
        Create a new JobContact.
    .EXAMPLE
        New-Sm8JobContact -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobContact" -Verb Post -Body $Body
}

function Update-Sm8JobContact {
    <#
    .SYNOPSIS
        Update an existing JobContact.
    .EXAMPLE
        Update-Sm8JobContact -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobContact" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8JobContact {
    <#
    .SYNOPSIS
        Delete (deactivate) a JobContact record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "JobContact" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_job_contacts, create: manage_job_contacts, delete/update: manage_job_contacts


# ---------------------------------------------------------------------------
# JobMaterial
# ---------------------------------------------------------------------------

function Get-Sm8JobMaterial {
    <#
    .SYNOPSIS
        List or retrieve JobMaterial records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8JobMaterial -All
    .EXAMPLE
        Get-Sm8JobMaterial -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "JobMaterial" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8JobMaterial {
    <#
    .SYNOPSIS
        Create a new JobMaterial.
    .EXAMPLE
        New-Sm8JobMaterial -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobMaterial" -Verb Post -Body $Body
}

function Update-Sm8JobMaterial {
    <#
    .SYNOPSIS
        Update an existing JobMaterial.
    .EXAMPLE
        Update-Sm8JobMaterial -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobMaterial" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8JobMaterial {
    <#
    .SYNOPSIS
        Delete (deactivate) a JobMaterial record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "JobMaterial" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_job_materials, create: manage_job_materials, delete/update: manage_job_materials


# ---------------------------------------------------------------------------
# JobPayment
# ---------------------------------------------------------------------------

function Get-Sm8JobPayment {
    <#
    .SYNOPSIS
        List or retrieve JobPayment records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8JobPayment -All
    .EXAMPLE
        Get-Sm8JobPayment -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "JobPayment" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8JobPayment {
    <#
    .SYNOPSIS
        Create a new JobPayment.
    .EXAMPLE
        New-Sm8JobPayment -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobPayment" -Verb Post -Body $Body
}

function Update-Sm8JobPayment {
    <#
    .SYNOPSIS
        Update an existing JobPayment.
    .EXAMPLE
        Update-Sm8JobPayment -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobPayment" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8JobPayment {
    <#
    .SYNOPSIS
        Delete (deactivate) a JobPayment record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "JobPayment" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_job_payments, create: manage_job_payments, delete/update: manage_job_payments


# ---------------------------------------------------------------------------
# JobQueue
# ---------------------------------------------------------------------------

function Get-Sm8JobQueue {
    <#
    .SYNOPSIS
        List or retrieve JobQueue records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8JobQueue -All
    .EXAMPLE
        Get-Sm8JobQueue -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "JobQueue" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8JobQueue {
    <#
    .SYNOPSIS
        Create a new JobQueue.
    .EXAMPLE
        New-Sm8JobQueue -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobQueue" -Verb Post -Body $Body
}

function Update-Sm8JobQueue {
    <#
    .SYNOPSIS
        Update an existing JobQueue.
    .EXAMPLE
        Update-Sm8JobQueue -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobQueue" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8JobQueue {
    <#
    .SYNOPSIS
        Delete (deactivate) a JobQueue record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "JobQueue" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_job_queues, create: manage_job_queues, delete/update: manage_job_queues


# ---------------------------------------------------------------------------
# JobTemplate
# ---------------------------------------------------------------------------

function Get-Sm8JobTemplate {
    <#
    .SYNOPSIS
        List or retrieve JobTemplate records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8JobTemplate -All
    .EXAMPLE
        Get-Sm8JobTemplate -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "JobTemplate" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8JobTemplate {
    <#
    .SYNOPSIS
        Create a new JobTemplate.
    .EXAMPLE
        New-Sm8JobTemplate -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobTemplate" -Verb Post -Body $Body
}

function Update-Sm8JobTemplate {
    <#
    .SYNOPSIS
        Update an existing JobTemplate.
    .EXAMPLE
        Update-Sm8JobTemplate -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "JobTemplate" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8JobTemplate {
    <#
    .SYNOPSIS
        Delete (deactivate) a JobTemplate record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "JobTemplate" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_jobs, create: create_jobs, delete/update: manage_jobs


# ---------------------------------------------------------------------------
# KnowledgeArticle
# ---------------------------------------------------------------------------

function Get-Sm8KnowledgeArticle {
    <#
    .SYNOPSIS
        List or retrieve KnowledgeArticle records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8KnowledgeArticle -All
    .EXAMPLE
        Get-Sm8KnowledgeArticle -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "KnowledgeArticle" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8KnowledgeArticle {
    <#
    .SYNOPSIS
        Create a new KnowledgeArticle.
    .EXAMPLE
        New-Sm8KnowledgeArticle -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "KnowledgeArticle" -Verb Post -Body $Body
}

function Update-Sm8KnowledgeArticle {
    <#
    .SYNOPSIS
        Update an existing KnowledgeArticle.
    .EXAMPLE
        Update-Sm8KnowledgeArticle -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "KnowledgeArticle" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8KnowledgeArticle {
    <#
    .SYNOPSIS
        Delete (deactivate) a KnowledgeArticle record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "KnowledgeArticle" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_knowledge, create: manage_knowledge, delete/update: manage_knowledge


# ---------------------------------------------------------------------------
# Location
# ---------------------------------------------------------------------------

function Get-Sm8Location {
    <#
    .SYNOPSIS
        List or retrieve Location records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Location -All
    .EXAMPLE
        Get-Sm8Location -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Location" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Location {
    <#
    .SYNOPSIS
        Create a new Location.
    .EXAMPLE
        New-Sm8Location -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Location" -Verb Post -Body $Body
}

function Update-Sm8Location {
    <#
    .SYNOPSIS
        Update an existing Location.
    .EXAMPLE
        Update-Sm8Location -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Location" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Location {
    <#
    .SYNOPSIS
        Delete (deactivate) a Location record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Location" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_locations, create: manage_locations, delete/update: manage_locations


# ---------------------------------------------------------------------------
# Material
# ---------------------------------------------------------------------------

function Get-Sm8Material {
    <#
    .SYNOPSIS
        List or retrieve Material records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Material -All
    .EXAMPLE
        Get-Sm8Material -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Material" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Material {
    <#
    .SYNOPSIS
        Create a new Material.
    .EXAMPLE
        New-Sm8Material -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Material" -Verb Post -Body $Body
}

function Update-Sm8Material {
    <#
    .SYNOPSIS
        Update an existing Material.
    .EXAMPLE
        Update-Sm8Material -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Material" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Material {
    <#
    .SYNOPSIS
        Delete (deactivate) a Material record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Material" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_inventory, create: create_inventory, delete/update: manage_inventory


# ---------------------------------------------------------------------------
# Note
# ---------------------------------------------------------------------------

function Get-Sm8Note {
    <#
    .SYNOPSIS
        List or retrieve Note records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Note -All
    .EXAMPLE
        Get-Sm8Note -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Note" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Note {
    <#
    .SYNOPSIS
        Create a new Note.
    .EXAMPLE
        New-Sm8Note -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Note" -Verb Post -Body $Body
}

function Update-Sm8Note {
    <#
    .SYNOPSIS
        Update an existing Note.
    .EXAMPLE
        Update-Sm8Note -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Note" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Note {
    <#
    .SYNOPSIS
        Delete (deactivate) a Note record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Note" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_job_notes, create: publish_job_notes, delete/update: publish_job_notes


# ---------------------------------------------------------------------------
# SecurityRole
# ---------------------------------------------------------------------------

function Get-Sm8SecurityRole {
    <#
    .SYNOPSIS
        List or retrieve SecurityRole records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8SecurityRole -All
    .EXAMPLE
        Get-Sm8SecurityRole -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "SecurityRole" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

    # scopes -> read: read_security_roles, create: read_security_roles, delete/update: read_security_roles


# ---------------------------------------------------------------------------
# Smstemplate
# ---------------------------------------------------------------------------

function Get-Sm8Smstemplate {
    <#
    .SYNOPSIS
        List or retrieve Smstemplate records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Smstemplate -All
    .EXAMPLE
        Get-Sm8Smstemplate -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Smstemplate" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

    # scopes -> read: manage_templates, create: manage_templates, delete/update: manage_templates


# ---------------------------------------------------------------------------
# StaffMember
# ---------------------------------------------------------------------------

function Get-Sm8StaffMember {
    <#
    .SYNOPSIS
        List or retrieve StaffMember records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8StaffMember -All
    .EXAMPLE
        Get-Sm8StaffMember -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "StaffMember" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8StaffMember {
    <#
    .SYNOPSIS
        Create a new StaffMember.
    .EXAMPLE
        New-Sm8StaffMember -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "StaffMember" -Verb Post -Body $Body
}

function Update-Sm8StaffMember {
    <#
    .SYNOPSIS
        Update an existing StaffMember.
    .EXAMPLE
        Update-Sm8StaffMember -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "StaffMember" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8StaffMember {
    <#
    .SYNOPSIS
        Delete (deactivate) a StaffMember record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "StaffMember" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_staff, create: manage_staff, delete/update: manage_staff


# ---------------------------------------------------------------------------
# StaffMessage
# ---------------------------------------------------------------------------

function Get-Sm8StaffMessage {
    <#
    .SYNOPSIS
        List or retrieve StaffMessage records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8StaffMessage -All
    .EXAMPLE
        Get-Sm8StaffMessage -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "StaffMessage" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8StaffMessage {
    <#
    .SYNOPSIS
        Create a new StaffMessage.
    .EXAMPLE
        New-Sm8StaffMessage -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "StaffMessage" -Verb Post -Body $Body
}

function Update-Sm8StaffMessage {
    <#
    .SYNOPSIS
        Update an existing StaffMessage.
    .EXAMPLE
        Update-Sm8StaffMessage -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "StaffMessage" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8StaffMessage {
    <#
    .SYNOPSIS
        Delete (deactivate) a StaffMessage record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "StaffMessage" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_messages, create: publish_messages, delete/update: publish_messages


# ---------------------------------------------------------------------------
# Supplier
# ---------------------------------------------------------------------------

function Get-Sm8Supplier {
    <#
    .SYNOPSIS
        List or retrieve Supplier records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Supplier -All
    .EXAMPLE
        Get-Sm8Supplier -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Supplier" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Supplier {
    <#
    .SYNOPSIS
        Create a new Supplier.
    .EXAMPLE
        New-Sm8Supplier -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Supplier" -Verb Post -Body $Body
}

function Update-Sm8Supplier {
    <#
    .SYNOPSIS
        Update an existing Supplier.
    .EXAMPLE
        Update-Sm8Supplier -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Supplier" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Supplier {
    <#
    .SYNOPSIS
        Delete (deactivate) a Supplier record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Supplier" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_suppliers, create: manage_suppliers, delete/update: manage_suppliers


# ---------------------------------------------------------------------------
# Task
# ---------------------------------------------------------------------------

function Get-Sm8Task {
    <#
    .SYNOPSIS
        List or retrieve Task records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8Task -All
    .EXAMPLE
        Get-Sm8Task -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "Task" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8Task {
    <#
    .SYNOPSIS
        Create a new Task.
    .EXAMPLE
        New-Sm8Task -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Task" -Verb Post -Body $Body
}

function Update-Sm8Task {
    <#
    .SYNOPSIS
        Update an existing Task.
    .EXAMPLE
        Update-Sm8Task -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "Task" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8Task {
    <#
    .SYNOPSIS
        Delete (deactivate) a Task record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "Task" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_tasks, create: manage_tasks, delete/update: manage_tasks


# ---------------------------------------------------------------------------
# TaxRate
# ---------------------------------------------------------------------------

function Get-Sm8TaxRate {
    <#
    .SYNOPSIS
        List or retrieve TaxRate records.
    .DESCRIPTION
        Retrieve a single record by -Uuid, or list records. With -All
        every record is returned (pagination is followed automatically);
        without it the first page is returned.
    .EXAMPLE
        Get-Sm8TaxRate -All
    .EXAMPLE
        Get-Sm8TaxRate -Uuid '123e4567-c6e7-7d94-8a1f-2c225b01e65b'
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject[]])]
    param(
        [string] $Uuid,
        [switch] $All,
        [hashtable] $Query
    )

    return Invoke-Sm8Resource -Resource "TaxRate" -Verb Get -Uuid $Uuid -All:$All -Query $Query
}

function New-Sm8TaxRate {
    <#
    .SYNOPSIS
        Create a new TaxRate.
    .EXAMPLE
        New-Sm8TaxRate -Body @{ date = '2026-09-09' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "TaxRate" -Verb Post -Body $Body
}

function Update-Sm8TaxRate {
    <#
    .SYNOPSIS
        Update an existing TaxRate.
    .EXAMPLE
        Update-Sm8TaxRate -Uuid '123e4567-...' -Body @{ date = '2026-09-10' }
    #>
    [CmdletBinding(SupportsShouldProcess)]
    [OutputType([pscustomobject])]
    param(
        [Parameter(Mandatory)] [string] $Uuid,
        [Parameter(Mandatory)] [object] $Body
    )

    return Invoke-Sm8Resource -Resource "TaxRate" -Verb Update -Uuid $Uuid -Body $Body
}

function Remove-Sm8TaxRate {
    <#
    .SYNOPSIS
        Delete (deactivate) a TaxRate record (sets active=0).
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact='Medium')]
    param(
        [Parameter(Mandatory, ValueFromPipeline)] [string] $Uuid
    )
    process {
        return Invoke-Sm8Resource -Resource "TaxRate" -Verb Delete -Uuid $Uuid
    }
}

    # scopes -> read: read_tax_rates, create: create_tax_rates, delete/update: manage_tax_rates

Export-ModuleMember -Function Get-Sm8AllocationWindow, New-Sm8AllocationWindow, Update-Sm8AllocationWindow, Remove-Sm8AllocationWindow, Get-Sm8Asset, New-Sm8Asset, Update-Sm8Asset, Remove-Sm8Asset, Get-Sm8AssetType, New-Sm8AssetType, Update-Sm8AssetType, Remove-Sm8AssetType, Get-Sm8AssetTypeField, New-Sm8AssetTypeField, Update-Sm8AssetTypeField, Remove-Sm8AssetTypeField, Get-Sm8Attachment, New-Sm8Attachment, Update-Sm8Attachment, Remove-Sm8Attachment, Get-Sm8Availability, New-Sm8Availability, Update-Sm8Availability, Remove-Sm8Availability, Get-Sm8Badge, Get-Sm8Bundle, New-Sm8Bundle, Update-Sm8Bundle, Remove-Sm8Bundle, Get-Sm8Category, New-Sm8Category, Update-Sm8Category, Remove-Sm8Category, Get-Sm8Client, New-Sm8Client, Update-Sm8Client, Remove-Sm8Client, Get-Sm8Company, New-Sm8Company, Update-Sm8Company, Remove-Sm8Company, Get-Sm8CompanyContact, New-Sm8CompanyContact, Update-Sm8CompanyContact, Remove-Sm8CompanyContact, Get-Sm8DocumentTemplate, Get-Sm8EmailTemplate, Get-Sm8Feedback, New-Sm8Feedback, Update-Sm8Feedback, Remove-Sm8Feedback, Get-Sm8Form, New-Sm8Form, Update-Sm8Form, Remove-Sm8Form, Get-Sm8FormField, New-Sm8FormField, Update-Sm8FormField, Remove-Sm8FormField, Get-Sm8FormResponse, New-Sm8FormResponse, Update-Sm8FormResponse, Remove-Sm8FormResponse, Get-Sm8Job, New-Sm8Job, Update-Sm8Job, Remove-Sm8Job, Get-Sm8JobActivity, New-Sm8JobActivity, Update-Sm8JobActivity, Remove-Sm8JobActivity, Get-Sm8JobAllocation, New-Sm8JobAllocation, Update-Sm8JobAllocation, Remove-Sm8JobAllocation, Get-Sm8JobChecklist, New-Sm8JobChecklist, Update-Sm8JobChecklist, Remove-Sm8JobChecklist, Get-Sm8JobContact, New-Sm8JobContact, Update-Sm8JobContact, Remove-Sm8JobContact, Get-Sm8JobMaterial, New-Sm8JobMaterial, Update-Sm8JobMaterial, Remove-Sm8JobMaterial, Get-Sm8JobPayment, New-Sm8JobPayment, Update-Sm8JobPayment, Remove-Sm8JobPayment, Get-Sm8JobQueue, New-Sm8JobQueue, Update-Sm8JobQueue, Remove-Sm8JobQueue, Get-Sm8JobTemplate, New-Sm8JobTemplate, Update-Sm8JobTemplate, Remove-Sm8JobTemplate, Get-Sm8KnowledgeArticle, New-Sm8KnowledgeArticle, Update-Sm8KnowledgeArticle, Remove-Sm8KnowledgeArticle, Get-Sm8Location, New-Sm8Location, Update-Sm8Location, Remove-Sm8Location, Get-Sm8Material, New-Sm8Material, Update-Sm8Material, Remove-Sm8Material, Get-Sm8Note, New-Sm8Note, Update-Sm8Note, Remove-Sm8Note, Get-Sm8SecurityRole, Get-Sm8Smstemplate, Get-Sm8StaffMember, New-Sm8StaffMember, Update-Sm8StaffMember, Remove-Sm8StaffMember, Get-Sm8StaffMessage, New-Sm8StaffMessage, Update-Sm8StaffMessage, Remove-Sm8StaffMessage, Get-Sm8Supplier, New-Sm8Supplier, Update-Sm8Supplier, Remove-Sm8Supplier, Get-Sm8Task, New-Sm8Task, Update-Sm8Task, Remove-Sm8Task, Get-Sm8TaxRate, New-Sm8TaxRate, Update-Sm8TaxRate, Remove-Sm8TaxRate
